# Docusaurus文档文件使用指南

## 核心问题：这几个文件到底有什么用？

### 1. 主文档源文件 (`docs/intro.mdx`)
- **用途**：这是**最主要的文档源文件**，用于编写和维护文档内容
- **特点**：使用MDX格式，支持Markdown和JSX语法
- **地位**：相当于文档的"母版"，所有多语言版本都基于此派生
- **注意**：这是您**应该主要编辑的文件**

### 2. 中文翻译文件 (`i18n/zh-CN/docusaurus-plugin-content-docs/current/intro.md`)
- **用途**：这是中文本地化的翻译文件
- **特点**：纯Markdown格式
- **地位**：用于覆盖默认语言的特定内容
- **注意**：只有当您需要为中文版本提供与默认语言不同的内容时才需要编辑

### 3. 英文翻译文件 (`i18n/en/docusaurus-plugin-content-docs/current/intro.md`)
- **用途**：这是英文本地化的翻译文件
- **特点**：纯Markdown格式
- **地位**：用于覆盖默认语言的特定内容
- **注意**：只有当您需要为英文版本提供与默认语言不同的内容时才需要编辑

## 部署时实际使用的文件

### 构建流程
当您运行 `npm run build` 时，Docusaurus会：
1. 读取 `docs/` 目录下的所有文档文件（包括MDX和Markdown）
2. 读取 `i18n/` 目录下的翻译文件
3. 将所有内容编译成静态HTML文件，存储在 `build/` 目录中

### 实际部署内容
部署时，**只有 `build/` 目录**被上传到服务器：
- `build/docs/` - 默认语言（中文）的文档
- `build/en/docs/` - 英文文档
- `build/assets/` - 静态资源（CSS、JS等）
- `build/img/` - 图片资源

## 如何正确修改文档

### 场景1：所有语言版本内容相同
✅ **推荐**：只需修改 `docs/intro.mdx`
- 所有语言版本都会自动继承这些更改
- 保持内容一致性
- 减少维护工作量

### 场景2：某个语言版本需要特殊内容
✅ **推荐**：
1. 先修改 `docs/intro.mdx` 作为基础
2. 然后在对应的翻译文件中覆盖需要不同的部分
3. 例如，英文版本需要不同的图片，就修改 `i18n/en/.../intro.md`

### 场景3：修改左侧导航结构
✅ **推荐**：修改 `sidebars.js` 文件
- 这里定义了所有文档的导航结构
- 支持分类、子菜单等复杂结构

## 多文档结构说明

我们已经实现了类似官方文档的多文档结构：
```
docs/
├── system-overview/      # 系统概述相关文档
├── quick-start/         # 快速开始相关文档  
├── core-features/       # 核心功能相关文档
├── technical-support/   # 技术与支持相关文档
└── about-system/        # 关于系统相关文档
```

- 每个目录对应一个导航分类
- 每个文件对应一个可点击的导航项
- 点击左侧导航项会跳转到对应的文档页面

## 最佳实践

1. **始终以 `docs/` 目录为主要编辑位置**
2. **只有在必要时才使用翻译文件**（特定语言的特殊内容）
3. **定期运行 `npm run build` 检查构建结果**
4. **使用 `npm start` 在本地预览修改效果**

## 常见误区

❌ **错误**：同时修改所有三个文件
- 导致内容不一致
- 增加维护工作量

❌ **错误**：只修改翻译文件而不修改源文件
- 默认语言版本不会更新
- 翻译文件可能被覆盖

❌ **错误**：直接修改 `build/` 目录下的文件
- 这些是编译后的文件，会被下次构建覆盖

---

**总结**：
- `docs/` 目录：主要编辑位置
- `i18n/` 目录：仅用于特定语言的内容覆盖
- `build/` 目录：部署时使用的最终静态文件
- `sidebars.js`：控制导航结构

现在您应该清楚这几个文件的关系和使用方法了！